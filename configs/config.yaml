http:
  port: "8080"

pipeline:
  worker_count: 4
  queue_size: 1000

detector:
  type: "iforest"
  window_size: 50
  threshold: 3.0
  onnx_model_path: "models/iforest.onnx"

alerts:
  slack_webhook: ""
  webhook: ""

storage:
  sqlite_path: "data/m2loganalyzer.db"

training:
  python_train_script: "tools/train_iforest.py"
  training_features: ["length"]  # example: features to use from logs
